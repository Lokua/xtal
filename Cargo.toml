[workspace]
members = ["lattice", "lattice-macros", "sketches"]
resolver = "3"
default-members = ["sketches"]

# # clean with:
# #   rm -rf target/incremental
# [profile.release]
# incremental = true
